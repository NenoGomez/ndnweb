{
  "name": "ndn-website-static",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build && vite preview --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/ && cp favicon.svg dist/ 2>/dev/null || :",
    "start": "vite preview --port=3000 --host=0.0.0.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
